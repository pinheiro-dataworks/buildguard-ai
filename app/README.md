# App (Streamlit) — reserved for Phase 8

Deliberately empty. Per the project roadmap, the UI is built only after the
ML evaluation design (data → features → baselines → calibrated models →
uncertainty → explainability) is stable — see
`BUILDGUARD_AI_PROJECT_SCOPE.md` Section 45.

When this phase starts, see
[`docs/design/UI_DESIGN_SPEC.md`](../docs/design/UI_DESIGN_SPEC.md) for the
agreed sidebar/branding/color direction before writing any page code.
